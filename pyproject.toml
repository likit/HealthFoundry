[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[project]
name = "healthfoundry"
version = "0.1.0"
description = "A research platform for constructing realistic synthetic healthcare worlds."
requires-python = ">=3.11"

[tool.setuptools.packages.find]
where = ["src"]

